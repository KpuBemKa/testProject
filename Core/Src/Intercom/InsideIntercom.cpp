/* #include "Intercom/InsideIntercom.h"

InsideIntercom::InsideIntercom(Mode *mode) : Intercom(mode)
{
}

bool InsideIntercom::CheckKey(uint32_t key)
{
    return this->mode->InsideCheckKey(key);
} */